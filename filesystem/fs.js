const fs = require('fs'); //file system module is a non-global module we need to import tham

//writeFileSync function is used to create a file 
//fs.writeFileSync("hii.txt","hello my self ruturaj this is node repo"); 

//__dirname is used to get the directory path
//console.log(__dirname);

//unlinkSync function is used to delete the file
//fs.unlinkSync("hii.txt");